package ru.dins.scalaschool.file_to_map.bot.api.model

import io.circe.{Decoder, HCursor}

//case class FileContent(content: String)
//
//object FileContent {
//  implicit val resultDecoder: Decoder[FileContent] =
//    (c: HCursor) => {
//      for {
////        fileType <- c.get[String]("type")
////        mtime <- c.get[Int]("mtime")
//        content <- c.get[String]("bytes")
//      } yield FileContent(content)
//    }
//}
